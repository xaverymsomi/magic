/*
gsap
.timeline({
    repeat: -1,
    yoyo: true,
    defaults: {
      duration: 1.5
    }
  })
.to('#rahisi', {
  duration: 1.5,
  x: 200,
  scale: 2,
  ease: 'bounce'
})
*/
gsap.to('#rahisi',{duration:1.5,x:200,ease:'back'})
